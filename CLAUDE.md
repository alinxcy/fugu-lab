# CLAUDE.md — fugu-lab

Sakana Fugu を月額契約すべきか判断するための計測環境。
`claudePlayGround` から `playground-spawn` で派生。由来は `.claude/SKILLS_ORIGIN.md`。

**このリポジトリは開発対象です。** playground と違い、自由に編集・追加・コミットしてよい。

## 何を作っているか

| ディレクトリ | 中身 | 仕様書 |
|---|---|---|
| `chat/` | Fugu を叩くチャットアプリ。使用データを JSONL に記録する | `chat/SPEC.md` |
| `dashboard/` | その JSONL を読み、契約判断のための集計を出す | `dashboard/SPEC.md` |
| `schema/` | 両者の唯一の結合点。UsageRecord の定義 | `schema/usage-record.md` |

**実装前に、担当ディレクトリの `SPEC.md` と `schema/usage-record.md` を必ず読むこと。**

## 最重要の制約 — 時間

Fugu を契約するかの判断期限は**日曜の朝**。測定窓は**金・土の 2 日間**しかない。

判断材料は「実際に使って溜まった使用データ」なので、**律速はビルド時間ではなく
データ蓄積時間**。`chat/` が動き出すのが半日遅れれば、判断材料が 1/4 失われる。

したがって:

- `chat/` の Phase 0 完了条件は「UI が良いこと」ではなく
  **「今日、日常の作業で自然に使い始められること」**
- 完成度より着手から測定開始までの時間を優先する
- UI の作り込み・リファクタは判断後にいくらでもできる。
  **失った測定日数だけは取り返せない**

## 絶対に壊してはいけないもの

`schema/usage-record.md` の「譲れない要件」。特に:

- **SSE 層でチャンクの `content` 以外を破棄しない。**
  ストリーミング時は `usage` が最終チャンクに載って届くことがあり、
  捨てると記録が成立せず、このプロジェクト全体が無意味になる。
- `raw_usage` の無加工保存(唯一の保険)
- `orchestration_*`(裏トークン内訳。契約判断の中心指標)
- null と 0 の区別

これらは「あとで直せばいい」類ではない。**記録されなかったデータは復元できない。**

## スコープ規律

各 `SPEC.md` に「やらないこと」が明記してある。**「あると親切だろう」と思っても
足さないこと。** 迷ったら入れない方に倒して確認する。

特に、集計・コスト計算・グラフは `dashboard/` の担当であり、
`chat/` の中に作らない(意図的な設計判断)。

## 進め方

- 実装前にスタックとファイル構成の方針を提示し、合意してから書き始める
- `parse_usage` と「チャンク列からの組み立て」は、ネットワーク不要の
  単体テストを書く。ここが壊れると全部無駄になる
- `chat/` を先に動かし、動き出してから `dashboard/` に着手する

## 判断後にやること

- `chat/` Phase 2: LM Studio アダプタ、tool 実行往復、UI 改善
- 一連の流れ(playground からの派生手順)の Skill 化 →
  `skill-harvester` / `skill-creator` を使う
